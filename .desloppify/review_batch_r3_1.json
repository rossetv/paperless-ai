{
  "assessments": {
    "cross_module_architecture": 82,
    "dependency_health": 88,
    "design_coherence": 80
  },
  "findings": [
    {
      "dimension": "cross_module_architecture",
      "identifier": "ocr-worker-reimplements-tag-management",
      "summary": "OCR worker._update_paperless_document manually manages pipeline tags instead of using common.tags.clean_pipeline_tags like the classifier worker does",
      "related_files": ["src/ocr/worker.py", "src/classifier/worker.py", "src/common/tags.py"],
      "evidence": ["ocr/worker.py lines 197-200 manually discards PRE_TAG_ID and OCR_PROCESSING_TAG_ID and adds POST_TAG_ID, while classifier/worker.py line 278 delegates to clean_pipeline_tags(). If a new pipeline tag is added to Settings, ocr/worker.py will silently fail to strip it."],
      "suggestion": "Replace the manual tag manipulation in _update_paperless_document with clean_pipeline_tags() followed by adding POST_TAG_ID, matching the classifier worker pattern.",
      "confidence": "high"
    },
    {
      "dimension": "cross_module_architecture",
      "identifier": "preflight-imports-private-holder",
      "summary": "preflight.py imports the private _openai_holder from llm.py, bypassing the module's public API",
      "related_files": ["src/common/preflight.py", "src/common/llm.py"],
      "evidence": ["preflight.py line 10: 'from .llm import _openai_holder' — the leading underscore signals internal use, yet preflight reaches in directly. The module exposes init_openai_client() publicly but has no corresponding public get accessor."],
      "suggestion": "Add a public get_openai_client() function in llm.py and have preflight.py use that instead of importing the private holder.",
      "confidence": "high"
    },
    {
      "dimension": "cross_module_architecture",
      "identifier": "classifier-constants-re-export-alias",
      "summary": "classifier/constants.py re-exports REFUSAL_PHRASES as ERROR_PHRASES creating a redundant alias",
      "related_files": ["src/classifier/constants.py", "src/common/constants.py", "src/classifier/quality_gates.py"],
      "evidence": ["classifier/constants.py line 48: 'ERROR_PHRASES: tuple[str, ...] = REFUSAL_PHRASES' — this is a simple alias with no transformation. quality_gates.py then imports ERROR_PHRASES. The rename adds cognitive overhead without semantic benefit since both names refer to the same refusal phrases."],
      "suggestion": "Have quality_gates.py import REFUSAL_PHRASES directly from common.constants and remove the ERROR_PHRASES alias from classifier/constants.py.",
      "confidence": "high"
    },
    {
      "dimension": "dependency_health",
      "identifier": "openai-sdk-used-for-ollama",
      "summary": "The openai SDK is a heavy transitive dependency (pulls in httpx, pydantic, etc.) used even in Ollama-only deployments where a lighter HTTP client would suffice",
      "related_files": ["src/common/library_setup.py", "src/common/llm.py"],
      "evidence": ["library_setup.py creates an openai.OpenAI client with api_key='dummy' and base_url pointing to Ollama. The full openai SDK (with pydantic models, type stubs, etc.) is loaded even though Ollama only needs a simple chat completions POST. httpx is already a direct dependency."],
      "suggestion": "Acceptable trade-off for API compatibility, but worth documenting in pyproject.toml that openai is required even for Ollama deployments.",
      "confidence": "high"
    },
    {
      "dimension": "design_coherence",
      "identifier": "stats-reset-split-across-layers",
      "summary": "Stats reset responsibility is inconsistently placed: OCR worker calls reset_stats() after claiming the lock, classifier worker calls it before refreshing the document",
      "related_files": ["src/ocr/worker.py", "src/classifier/worker.py"],
      "evidence": ["classifier/worker.py line 84: reset_stats() is called at the top of process() before get_document(). ocr/worker.py line 80: reset_stats() is called after claim_processing_tag succeeds. This means an OCR early-exit (error tag check) leaves stale stats, while classifier always starts clean."],
      "suggestion": "Move reset_stats() to a consistent position in both workers — either as the first action in process() (matching classifier) or immediately before the first provider call.",
      "confidence": "high"
    },
    {
      "dimension": "design_coherence",
      "identifier": "daemon-process-document-shape-mismatch",
      "summary": "OCR and classifier daemons use different patterns for per-document processing closures, making the shared daemon_loop harder to reason about",
      "related_files": ["src/ocr/daemon.py", "src/classifier/daemon.py"],
      "evidence": ["ocr/daemon.py defines _process_document as a module-level function and wraps it in a lambda (line 67). classifier/daemon.py defines process_document as a closure inside main() capturing taxonomy_cache. The module-level vs closure asymmetry is a minor coherence gap."],
      "suggestion": "Adopt a consistent pattern — either both use closures capturing shared state or both use module-level functions with explicit parameters.",
      "confidence": "high"
    },
    {
      "dimension": "design_coherence",
      "identifier": "worker-constructor-unpacks-doc-fields",
      "summary": "Both worker classes extract doc_id and title from the doc dict in __init__ but also store the full doc dict, creating partially-redundant state",
      "related_files": ["src/ocr/worker.py", "src/classifier/worker.py"],
      "evidence": ["classifier/worker.py lines 58-64 stores self.doc, self.doc_id = doc['id'], self.title = doc.get('title'). ocr/worker.py lines 58-64 does the same, plus later reassigns self.doc = document (line 63 of process). The doc_id/title caching is fine for logging but the pattern of re-fetching and overwriting self.doc mid-flight (OCR worker line 63) is surprising."],
      "suggestion": "In the OCR worker, stop overwriting self.doc after re-fetch, or rename the local to 'document' consistently and drop self.doc storage.",
      "confidence": "high"
    }
  ]
}
