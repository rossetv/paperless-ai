{
  "assessments": {
    "design_coherence": 84,
    "abstraction_fitness": 82,
    "convention_outlier": 82
  },
  "findings": [
    {
      "dimension": "design_coherence",
      "identifier": "redundant-reset-stats-on-fresh-provider",
      "summary": "Both OcrProcessor.process() and ClassificationProcessor.process() call reset_stats() on providers that are created fresh per-document in the daemon entry points, making the reset a no-op on already-zero counters",
      "related_files": ["src/ocr/worker.py", "src/ocr/daemon.py", "src/classifier/worker.py", "src/classifier/daemon.py"],
      "evidence": [
        "ocr/daemon.py line 21: OcrProvider(settings) created fresh per _process_document call; worker.py line 59: self.ocr_provider.reset_stats() zeros already-zero counters",
        "classifier/daemon.py line 68: ClassificationProvider(settings) created fresh per process_document call; worker.py line 82: self.classifier.reset_stats() zeros already-zero counters"
      ],
      "suggestion": "Remove reset_stats() calls from both worker process() methods since providers are instantiated fresh per document, or make providers shared across a batch and reset at batch boundaries to make the call meaningful",
      "confidence": "high"
    },
    {
      "dimension": "design_coherence",
      "identifier": "ocr-processor-process-method-nesting",
      "summary": "OcrProcessor.process() has deep nesting with a try/finally containing multiple early returns, nested try/except/finally blocks, and conditional error paths making the control flow harder to follow than its ClassificationProcessor counterpart",
      "related_files": ["src/ocr/worker.py"],
      "evidence": [
        "Lines 50-135: outer try/finally with 'claimed' flag, inner try/except for image conversion (lines 83-91), inner try/finally for image cleanup (lines 97-104), then conditional error handling (lines 106-111) before finally assembling text",
        "Three levels of nesting in the main processing path versus ClassificationProcessor which has a flatter structure"
      ],
      "suggestion": "Extract image lifecycle management (download, convert, close) into a context manager or dedicated method to reduce nesting depth in process()",
      "confidence": "medium"
    },
    {
      "dimension": "design_coherence",
      "identifier": "settings-class-monolith",
      "summary": "Settings class bundles 30+ configuration fields for both OCR and classification daemons in a single class, where each daemon only uses a subset of the fields",
      "related_files": ["src/common/config.py"],
      "evidence": [
        "Lines 15-59: Settings has OCR-specific fields (OCR_DPI, OCR_MAX_SIDE, PAGE_WORKERS), classification-specific fields (CLASSIFY_PERSON_FIELD_ID, CLASSIFY_MAX_CHARS, etc.), and shared fields — all loaded unconditionally",
        "The OCR daemon never uses CLASSIFY_* fields and vice versa, yet both must load all environment variables"
      ],
      "suggestion": "Consider splitting into a base SharedSettings with OcrSettings and ClassifySettings subclasses, or accept the cost as low given it is a single-file configuration loader with clear grouping methods",
      "confidence": "low"
    },
    {
      "dimension": "abstraction_fitness",
      "identifier": "init-openai-client-thin-passthrough",
      "summary": "init_openai_client() is a one-line passthrough to _openai_holder.init() that adds no validation, logging, or translation, but forms part of a coherent three-function public facade over the private singleton",
      "related_files": ["src/common/llm.py", "src/common/library_setup.py"],
      "evidence": [
        "llm.py lines 48-49: def init_openai_client(client): _openai_holder.init(client) — no added logic",
        "get_openai_client() (line 52) and is_openai_client_ready() (line 57) are similar thin wrappers forming a consistent public API boundary",
        "library_setup.py and preflight.py both use these public functions rather than accessing _openai_holder directly"
      ],
      "suggestion": "The three functions form a coherent public facade over the private _OpenAIClientHolder singleton. The wrapper cost is low and the encapsulation is justified. No change needed.",
      "confidence": "low"
    },
    {
      "dimension": "abstraction_fitness",
      "identifier": "taxonomy-tag-creation-diverges-from-kind-pattern",
      "summary": "TaxonomyCache uses _TaxonomyKind NamedTuple to parameterise _get_or_create_item_id for correspondents and document types, but get_or_create_tag_ids duplicates similar logic inline instead of using the same abstraction",
      "related_files": ["src/classifier/taxonomy.py"],
      "evidence": [
        "Lines 180-209: _get_or_create_item_id uses kind_factory pattern with _match_item, creator, refresh-on-failure retry — clean abstraction",
        "Lines 217-248: get_or_create_tag_ids reimplements the same match-or-create-with-refresh pattern inline, but with an additional loop and matching_algorithm inference",
        "The divergence exists because tags have batch semantics (multiple tags per call) while correspondents/types are singular, but the inner create-and-retry logic is identical"
      ],
      "suggestion": "Extract the single-item match-or-create-with-refresh logic from get_or_create_tag_ids to reuse _get_or_create_item_id, wrapping it in a loop. The matching_algorithm can be passed through the _TaxonomyKind.creator callable.",
      "confidence": "medium"
    },
    {
      "dimension": "abstraction_fitness",
      "identifier": "daemon-loop-exception-tuple-not-composed",
      "summary": "run_polling_threadpool catches a manually curated list of 7 exception types that partially overlaps with PAPERLESS_CALL_EXCEPTIONS but is not composed from it",
      "related_files": ["src/common/daemon_loop.py", "src/common/paperless.py"],
      "evidence": [
        "daemon_loop.py line 106: except (OSError, httpx.HTTPError, RuntimeError, ValueError, TypeError, KeyError) — 7 manually listed types",
        "paperless.py line 22: PAPERLESS_CALL_EXCEPTIONS = (OSError, httpx.HTTPError, ValueError, KeyError) — 4 types",
        "The daemon loop adds RuntimeError (from LLM holder) and TypeError (from type coercion) which are legitimate at this boundary"
      ],
      "suggestion": "Define a DAEMON_LOOP_EXCEPTIONS tuple that composes PAPERLESS_CALL_EXCEPTIONS with (RuntimeError, TypeError) to make the relationship explicit and reduce drift risk when PAPERLESS_CALL_EXCEPTIONS is updated",
      "confidence": "medium"
    },
    {
      "dimension": "convention_outlier",
      "identifier": "inconsistent-init-exports",
      "summary": "ocr/__init__.py and classifier/__init__.py export different subsets of their packages — ocr exports utility functions (bytes_to_images, assemble_full_text) while classifier does not export its utilities",
      "related_files": ["src/ocr/__init__.py", "src/classifier/__init__.py"],
      "evidence": [
        "ocr/__init__.py exports: OcrProcessor, OCR_ERROR_MARKER, OcrProvider, assemble_full_text, bytes_to_images — includes low-level utilities",
        "classifier/__init__.py exports: ClassificationProcessor, ClassificationProvider, ClassificationResult, TaxonomyCache, parse_classification_response — only high-level types",
        "Neither package's __init__.py is imported by the daemon entry points, which use direct submodule imports"
      ],
      "suggestion": "Align export strategy: either both packages export only high-level types (Processor, Provider) or both export all public symbols. Since daemon entry points import submodules directly, the __init__.py exports likely serve tests — make the convention consistent",
      "confidence": "medium"
    },
    {
      "dimension": "convention_outlier",
      "identifier": "update-document-vs-update-document-metadata-overlap",
      "summary": "PaperlessClient has two update methods (update_document and update_document_metadata) that both PATCH the same endpoint with overlapping functionality but different signatures",
      "related_files": ["src/common/paperless.py"],
      "evidence": [
        "Line 149: update_document(doc_id, content, new_tags) — takes content+tags as positional args, patches with {content, tags}",
        "Line 173: update_document_metadata(doc_id, **fields) — takes keyword args mapped through _METADATA_FIELDS, can also patch tags",
        "Both methods PATCH the same URL: /api/documents/{doc_id}/",
        "update_document is called from two places: tags.py finalize_document_with_error (when content is not None) and ocr/worker.py _update_paperless_document"
      ],
      "suggestion": "Consider unifying into a single update method with optional content parameter to eliminate the dual-path API. The _METADATA_FIELDS mapping in update_document_metadata is a good pattern that could absorb the content field.",
      "confidence": "medium"
    },
    {
      "dimension": "convention_outlier",
      "identifier": "common-no-init-exports",
      "summary": "common/__init__.py has no exports (only a docstring) while both domain packages define __all__ with re-exports, creating an inconsistency in how shared code is accessed",
      "related_files": ["src/common/__init__.py", "src/ocr/__init__.py", "src/classifier/__init__.py"],
      "evidence": [
        "common/__init__.py line 1: only contains a docstring — no re-exports",
        "All consumers import from common submodules directly (e.g., from common.config import Settings, from common.llm import OpenAIChatMixin)",
        "ocr/ and classifier/ both define __all__ with re-exports from their submodules"
      ],
      "suggestion": "This is a minor inconsistency. common/ is a utility package where direct submodule imports are the natural pattern, while ocr/ and classifier/ are domain packages with a cohesive public API. The difference is arguably intentional but worth documenting.",
      "confidence": "low"
    }
  ]
}
