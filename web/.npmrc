# Use the public npm registry. paperless-ai is a public open-source project and
# must build for everyone — CI, the Docker image build, and external
# contributors. Do not repoint this at an internal or corporate registry.
registry=https://registry.npmjs.org/
fund=false
