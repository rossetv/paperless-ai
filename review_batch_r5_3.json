{
  "assessments": {
    "design_coherence": 82,
    "abstraction_fitness": 79,
    "convention_outlier": 80
  },
  "findings": [
    {
      "dimension": "design_coherence",
      "identifier": "god_settings_class",
      "summary": "Single Settings class carries all config for both OCR and classifier daemons, mixing unrelated concerns",
      "related_files": ["src/common/config.py"],
      "evidence": [
        "Settings.__init__ loads 7 separate concern groups (_load_api_settings through _load_classification_settings) into one flat object with 30+ attributes",
        "OCR daemon carries CLASSIFY_* attributes it never uses; classifier daemon carries OCR_DPI, OCR_MAX_SIDE, PAGE_WORKERS it never uses",
        "Both ocr/daemon.py and classifier/daemon.py receive the full Settings object and pass it through to workers that only read a subset"
      ],
      "suggestion": "Extract a base DaemonSettings with shared fields, then OcrSettings and ClassifierSettings subclasses with daemon-specific attributes. Alternatively, use a frozen dataclass per concern group and compose them.",
      "confidence": "medium"
    },
    {
      "dimension": "design_coherence",
      "identifier": "parallel_daemon_structure",
      "summary": "OCR and classifier daemons have well-mirrored structure showing strong design coherence",
      "related_files": ["src/ocr/daemon.py", "src/classifier/daemon.py", "src/common/daemon_loop.py", "src/common/bootstrap.py"],
      "evidence": [
        "Both daemons follow identical patterns: bootstrap_daemon() -> run_polling_threadpool() with matching lambda signatures",
        "Shared bootstrap, polling loop, signal handling, and stale-lock recovery are correctly factored into common/",
        "Worker classes (OcrProcessor, ClassificationProcessor) follow the same lifecycle: claim tag -> process -> release tag"
      ],
      "suggestion": "No change needed; this is positive evidence of design coherence.",
      "confidence": "high"
    },
    {
      "dimension": "design_coherence",
      "identifier": "worker_finalize_error_signature_mismatch",
      "summary": "OcrProcessor._finalize_with_error accepts optional content kwarg while ClassificationProcessor._finalize_with_error does not",
      "related_files": ["src/ocr/worker.py", "src/classifier/worker.py"],
      "evidence": [
        "OcrProcessor._finalize_with_error(self, tags: set[int], *, content: str | None = None) has content parameter",
        "ClassificationProcessor._finalize_with_error(self, tags: set[int]) has no content parameter",
        "Both delegate to common.tags.finalize_document_with_error which accepts content as optional kwarg",
        "The asymmetry is justified since OCR may want to store partial error content while classifier does not"
      ],
      "suggestion": "The difference is functionally motivated. Consider adding a brief comment in the classifier version noting the intentional omission for clarity.",
      "confidence": "low"
    },
    {
      "dimension": "abstraction_fitness",
      "identifier": "openai_chat_mixin",
      "summary": "OpenAIChatMixin uses mixin pattern with implicit self.settings protocol dependency rather than explicit composition",
      "related_files": ["src/common/llm.py", "src/ocr/provider.py", "src/classifier/provider.py"],
      "evidence": [
        "OpenAIChatMixin expects self.settings to expose MAX_RETRIES and MAX_RETRY_BACKOFF_SECONDS but this is implicit via the retry decorator's RetrySettings Protocol",
        "Mixin also expects self._stats (initialized via _init_stats) and self._STAT_KEYS, creating a fragile init ordering requirement",
        "Both OcrProvider and ClassificationProvider must remember to call _init_stats() in their __init__"
      ],
      "suggestion": "Convert the mixin to a composed helper class that receives settings and stat keys explicitly in its constructor, eliminating implicit protocol dependencies.",
      "confidence": "medium"
    },
    {
      "dimension": "abstraction_fitness",
      "identifier": "openai_client_holder_indirection",
      "summary": "Module-level _OpenAIClientHolder singleton wraps a simple client reference through init/get/is_ready indirection",
      "related_files": ["src/common/llm.py", "src/common/library_setup.py"],
      "evidence": [
        "Three module-level functions (init_openai_client, get_openai_client, is_openai_client_ready) forward to _openai_holder methods that are trivial getters/setters",
        "_OpenAIClientHolder class exists to 'avoid a bare module-level mutable' per docstring, but creates the same global mutable pattern with more ceremony",
        "The holder has exactly one concrete usage path: library_setup.py calls init, then llm.py and preflight.py call get/is_ready"
      ],
      "suggestion": "A module-level Optional[openai.OpenAI] with init/get functions would be simpler and equally safe. The holder class adds indirection without additional safety.",
      "confidence": "medium"
    },
    {
      "dimension": "abstraction_fitness",
      "identifier": "taxonomy_kind_namedtuple",
      "summary": "TaxonomyCache uses _TaxonomyKind NamedTuple as an internal parameter bag to reduce repetition in _get_or_create_item_id",
      "related_files": ["src/classifier/taxonomy.py"],
      "evidence": [
        "_TaxonomyKind bundles 6 fields (items, mapping, normalizer, allow_substring, creator, label) into a tuple",
        "Used only by _get_or_create_item_id to avoid duplicating the lookup-or-create logic",
        "The factory callable pattern (_correspondent_kind, _document_type_kind) returning NamedTuples is somewhat unusual but avoids code duplication effectively"
      ],
      "suggestion": "This is a borderline case. The pattern is justified by the DRY benefit but could be clearer as a small internal dataclass or by passing the fields directly.",
      "confidence": "low"
    },
    {
      "dimension": "abstraction_fitness",
      "identifier": "llm_limiter_module_singleton",
      "summary": "LLMConcurrencyLimiter instantiated as module-level singleton requires init() call before use, creating temporal coupling",
      "related_files": ["src/common/concurrency.py", "src/common/bootstrap.py"],
      "evidence": [
        "llm_limiter = LLMConcurrencyLimiter() created at module scope in concurrency.py",
        "bootstrap.py calls llm_limiter.init(settings.LLM_MAX_CONCURRENT) during startup",
        "llm.py imports llm_limiter and calls .acquire() in _create_completion",
        "If init() is never called, _semaphore stays None and acquire() silently becomes a no-op, which is safe but may mask misconfiguration"
      ],
      "suggestion": "The current design is pragmatic since the no-op fallback matches the 'unlimited' semantics. No change strictly needed, but logging a warning on first acquire() if init() was never called would add observability.",
      "confidence": "low"
    },
    {
      "dimension": "convention_outlier",
      "identifier": "inconsistent_sentinel_organization",
      "summary": "Related sentinel strings (error markers and refusal phrases) are scattered across three locations with different organizational patterns",
      "related_files": ["src/ocr/text_assembly.py", "src/common/config.py", "src/common/constants.py"],
      "evidence": [
        "OCR_ERROR_MARKER = '[OCR ERROR]' is a module-level constant in ocr/text_assembly.py",
        "REFUSAL_MARK = 'CHATGPT REFUSED TO TRANSCRIBE' is a class attribute with default value on Settings in common/config.py",
        "REFUSAL_PHRASES is a module-level tuple in common/constants.py",
        "These three related sentinel strings follow three different organizational patterns despite serving the same domain concern (error/refusal detection)"
      ],
      "suggestion": "Consolidate all error/refusal sentinel strings into common/constants.py. REFUSAL_MARK could be a plain constant rather than a Settings class attribute since it is not user-configurable in practice (it is hardcoded as a default).",
      "confidence": "medium"
    },
    {
      "dimension": "convention_outlier",
      "identifier": "init_exports_inconsistency",
      "summary": "classifier/ and ocr/ packages have curated __init__.py re-exports while common/ has a docstring-only __init__.py",
      "related_files": ["src/common/__init__.py", "src/classifier/__init__.py", "src/ocr/__init__.py"],
      "evidence": [
        "classifier/__init__.py exports 5 symbols with __all__",
        "ocr/__init__.py exports 5 symbols with __all__",
        "common/__init__.py has only a docstring with no re-exports",
        "All imports from common/ use fully qualified module paths (common.config, common.paperless), which is actually good practice for a utility package"
      ],
      "suggestion": "The inconsistency is minor and arguably intentional: domain packages expose a curated API surface while the utility package encourages direct module imports. No change needed.",
      "confidence": "low"
    },
    {
      "dimension": "convention_outlier",
      "identifier": "mixed_import_styles",
      "summary": "Worker modules use absolute imports for common/ but relative imports for sibling modules within the same package",
      "related_files": ["src/classifier/worker.py", "src/ocr/worker.py", "src/classifier/provider.py", "src/ocr/provider.py"],
      "evidence": [
        "classifier/worker.py: 'from common.claims import claim_processing_tag' (absolute) alongside 'from .content_prep import ...' (relative)",
        "ocr/worker.py: 'from common.config import Settings' (absolute) alongside 'from .provider import OcrProvider' (relative)",
        "This pattern is consistent across all files: absolute for cross-package, relative for intra-package"
      ],
      "suggestion": "No change needed. The convention is internally consistent: absolute for cross-package imports, relative for intra-package imports. This is a Python best practice.",
      "confidence": "high"
    }
  ]
}
