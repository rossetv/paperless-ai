{
  "assessments": {
    "error_consistency": 87,
    "test_strategy": 88,
    "ai_generated_debt": 85
  },
  "findings": [
    {
      "dimension": "error_consistency",
      "identifier": "broad_daemon_loop_catch",
      "summary": "Daemon loop catches a wide tuple of exception types including TypeError/KeyError, swallowing them with only a log message",
      "related_files": ["src/common/daemon_loop.py"],
      "evidence": ["Line 106 catches (OSError, httpx.HTTPError, RuntimeError, ValueError, TypeError, KeyError) in the polling loop. TypeError and KeyError typically indicate programming bugs, not transient failures, yet they are treated identically to network errors and silently retried."],
      "suggestion": "Narrow the catch clause to transient exceptions (OSError, httpx.HTTPError, RuntimeError). Let TypeError and KeyError propagate so bugs are surfaced rather than silently retried in a loop.",
      "confidence": "medium"
    },
    {
      "dimension": "error_consistency",
      "identifier": "silent_swallow_in_tags",
      "summary": "Several tag-lifecycle helpers log exceptions but do not propagate or signal failure to callers",
      "related_files": ["src/common/tags.py"],
      "evidence": ["remove_stale_queue_tag (line 65-72), release_processing_tag (lines 93-113) catch PAPERLESS_CALL_EXCEPTIONS and only log. The callers have no way to know the operation failed. While this is intentional best-effort, the pattern is repeated verbatim across multiple functions."],
      "suggestion": "Return a bool success indicator from these helpers so callers can optionally act on failure, or at minimum centralize the catch-and-log-only pattern into a single wrapper to reduce repetition.",
      "confidence": "low"
    },
    {
      "dimension": "error_consistency",
      "identifier": "paperless_call_exceptions_includes_valueerror_keyerror",
      "summary": "PAPERLESS_CALL_EXCEPTIONS bundles ValueError and KeyError with network errors, broadening catch clauses beyond I/O boundaries",
      "related_files": ["src/common/paperless.py"],
      "evidence": ["PAPERLESS_CALL_EXCEPTIONS = (OSError, httpx.HTTPError, ValueError, KeyError) at line 23. Every caller that catches this tuple will also suppress programming errors like missing dict keys or bad type conversions, not just network/HTTP failures."],
      "suggestion": "Consider splitting into separate tuples: one for network/HTTP errors and one for response-parsing errors. Callers that need both can union them explicitly, making intent clearer.",
      "confidence": "medium"
    },
    {
      "dimension": "test_strategy",
      "identifier": "e2e_tests_still_mock_heavy",
      "summary": "E2E tests use stateful mocks rather than exercising real HTTP or subprocess boundaries",
      "related_files": ["tests/e2e/test_ocr_workflow.py", "tests/e2e/test_classifier_workflow.py"],
      "evidence": ["test_ocr_workflow.py builds a _make_stateful_client mock with side_effect functions to simulate tag state changes. While more realistic than unit mocks, these tests never touch a real HTTP layer or subprocess, making them closer to integration tests than true e2e tests."],
      "suggestion": "Consider renaming the e2e directory to integration_stateful or adding a note that true e2e tests require a running Paperless instance. This prevents false confidence about end-to-end coverage.",
      "confidence": "medium"
    },
    {
      "dimension": "test_strategy",
      "identifier": "no_test_for_bootstrap_daemon",
      "summary": "bootstrap_daemon orchestration has no dedicated tests despite being a critical startup path",
      "related_files": ["src/common/bootstrap.py", "tests/unit/common/test_bootstrap.py"],
      "evidence": ["test_bootstrap.py exists but bootstrap_daemon coordinates Settings creation, logging, library setup, signal handlers, preflight checks, and stale lock recovery. A failure in this sequence leaves the daemon silently dead (returns None). The coordination logic merits direct testing."],
      "suggestion": "Add tests that verify bootstrap_daemon returns None on ValueError from Settings, on PreflightError from preflight checks, and returns (settings, client) on success.",
      "confidence": "medium"
    },
    {
      "dimension": "ai_generated_debt",
      "identifier": "docstring_on_trivial_helpers",
      "summary": "Several trivial 1-3 line functions carry multi-line docstrings that restate the code",
      "related_files": ["src/common/shutdown.py", "src/common/llm.py", "src/common/concurrency.py"],
      "evidence": ["request_shutdown() is a one-liner (_shutdown_event.set()) yet the module-level docstring describes it. is_openai_client_ready() (return self._client is not None) has clear naming that makes the docstring 'Return whether the client is ready' redundant. LLMConcurrencyLimiter.acquire() docstring 'Acquire a slot; no-op when unlimited' restates the 4-line body."],
      "suggestion": "Trim docstrings on trivial functions where the name and signature already convey full intent. Reserve docstrings for non-obvious behavior or contract details.",
      "confidence": "low"
    },
    {
      "dimension": "ai_generated_debt",
      "identifier": "verbose_arrange_act_assert_comments",
      "summary": "Tests uniformly carry # Arrange / # Act / # Assert comments even on trivial 3-line tests",
      "related_files": ["tests/unit/common/test_claims.py", "tests/unit/common/test_daemon_loop.py", "tests/unit/ocr/test_worker.py"],
      "evidence": ["test_returns_true_immediately_when_tag_id_is_none in test_claims.py has 3 meaningful lines but carries Arrange/Act/Assert markers. This pattern is applied consistently across all test files regardless of test complexity, which is a hallmark of LLM-generated test code."],
      "suggestion": "Reserve section comments for complex tests with multi-step setup. For simple tests, the code is self-documenting without ceremony.",
      "confidence": "low"
    }
  ]
}
